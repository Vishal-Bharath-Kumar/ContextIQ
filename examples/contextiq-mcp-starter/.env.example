# Optional manual bearer token used by .vscode/mcp.json before the setup task runs.
CONTEXTIQ_TOKEN=

# Optional noninteractive dev-login fallback used by the refresh task.
CONTEXTIQ_DEV_USERNAME=admin
CONTEXTIQ_DEV_PASSWORD=