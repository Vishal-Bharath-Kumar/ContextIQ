apiVersion: v2
name:        minio
description: MinIO distributed object store for ContextIQ
type:        application
version:     0.1.0
dependencies:
  - name:       minio
    version:    "5.0.14"    # pinned; update after staging validation
    repository: https://charts.min.io
