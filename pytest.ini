[pytest]
testpaths = tests
addopts = -v
timeout = 120
asyncio_mode = auto
markers =
    security: JWT security attack-vector tests (run with -m security)
    benchmark: pytest-benchmark latency tests
