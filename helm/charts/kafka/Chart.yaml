apiVersion: v2
name:        kafka
description: Apache Kafka KRaft cluster for ContextIQ event streaming
type:        application
version:     0.1.0
dependencies:
  - name:       kafka
    version:    "29.3.13"    # pinned; update after staging validation
    repository: https://charts.bitnami.com/bitnami
