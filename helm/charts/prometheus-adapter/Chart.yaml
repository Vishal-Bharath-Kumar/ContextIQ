apiVersion: v2
name:        prometheus-adapter
description: Prometheus Adapter — bridges Prometheus metrics to the Kubernetes custom metrics API
type:        application
version:     0.1.0
dependencies:
  - name:       prometheus-adapter
    version:    "4.10.0"    # pinned; update after staging validation
    repository: https://prometheus-community.github.io/helm-charts
